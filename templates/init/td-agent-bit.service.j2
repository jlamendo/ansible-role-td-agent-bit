[Unit]
Description=TD Agent Bit
Requires=network.target
After=network.target

[Service]
Type=simple
ExecStart={{ flb_binary }} -c {{ flb_conf_dir }}/{{ flb_conf_file }}
Restart=always

[Install]
WantedBy=multi-user.target